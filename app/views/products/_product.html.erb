<% if product == @current_item %>
  <li class="line-item-highlight">
<% else %>
  <li>
<% end %>
  <%= button_to line_items_path(product_id: product), remote: true, form_class: "image-button" do %>
    <%= image_tag(product.image_url) %>
  <% end %>
  <h2><%= product.title %></h2>
  <p>
    <%= sanitize(product.description) %>
  </p>
  <div class="price">
    <%= number_to_currency(product.price, precision: 2, unit: "₴") %>
    <%= button_to 'Add to Cart', line_items_path(product_id: product), remote: true %>
  </div>
</li>
